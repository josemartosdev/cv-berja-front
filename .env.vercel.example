# Vercel → Project → Settings → Environment Variables (Production + Preview)
# Tras guardar: Redeploy obligatorio (Vite embebe VITE_* en el build)

VITE_API_URL=https://cv-berja-back-production.up.railway.app/api
